<% content_for(:title) { "New Issue — #{@book.title}" } %>

<% content_for :header do %>
  <nav>
    <%= link_to book_issues_path(@book), class: "btn flex-item-justify-start" do %>
      <%= image_tag "arrow-left.svg", aria: { hidden: true }, size: 24 %>
      <span class="for-screen-reader">Back to issues</span>
    <% end %>
  </nav>
<% end %>

<%= render "books/issues/form", book: @book, issue: @issue, available_leaves: @available_leaves %>
